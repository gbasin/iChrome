{{#title}}<h2 class="header">{{title}}</h2>{{/title}}

<div class="items">
	{{#tabs}}
		<a class="item" href="{{url}}" data-id="{{id}}">
			<img class="favicon" src="{{favicon}}" />
			<span class="title">{{title}}</span>
		</a>
	{{/tabs}}
</div>