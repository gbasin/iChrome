{{#title}}<h2 class="header">{{title}}</h2>{{/title}}

<div class="sites">
	{{#sites}}
		<a class="site" href="{{url}}{{#newTab}}" target="_blank{{/newTab}}">
			<img class="favicon" src="{{favicon}}" />
			<span class="title">{{title}}</span>
		</a>
	{{/sites}}
</div>