<h2 class="title">{{i18n.menu.widgets}}</h2>

<div class="content">
	{{#widgets}}
		<div data-id="{{id}}" class="widget">
			<img src="{{icon}}" />

			<h2 class="title">{{name}}</h2>

			<div class="desc">{{desc}}</div>
		</div>
	{{/widgets}}
</div>