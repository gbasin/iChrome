<template id="template.widgets.keep">
    <iframe src="{{url}}" style="height:{{height}}px" sandbox="allow-same-origin allow-popups allow-forms allow-scripts" seamless>For some strange reason it seems your browser doesn't support iFrames!</iframe>
</template>
